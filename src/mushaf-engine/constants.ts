export const PAGE_HEIGHT_OVER_WIDTH = 1.6;

export const DEFAULT_BUFFER_BEHIND = 2;
export const DEFAULT_BUFFER_AHEAD = 5;

export const DEFAULT_PAGE_GAP_PX = 12;

export const DEFAULT_MAX_PAGE_WIDTH_SCROLL_PX = 980;
export const DEFAULT_MAX_PAGE_WIDTH_SWIPE_PX = 760;

export const DEFAULT_FONT_CACHE_BUDGET_BYTES = 50 * 1024 * 1024;
export const DEFAULT_PAGE_PAYLOAD_CACHE_LIMIT = 80;

// Coordinate space for payload x/width. Update if/when assets define a different space.
export const DEFAULT_COORD_WIDTH = 100;

